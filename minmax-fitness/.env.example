# Base URL of the sync/auth API (without trailing slash)
# The app automatically appends /sync/pull, /sync/push, /auth/register, etc.
EXPO_PUBLIC_SYNC_API_URL="https://minmax-fitness.vercel.app/sync"

# Sentry DSN for crash reporting (optional — leave blank to disable)
EXPO_PUBLIC_SENTRY_DSN=""
